diff --git a/lmcache/v1/config.py b/lmcache/v1/config.py
index cd2ada1..6b9e49c 100644
--- a/lmcache/v1/config.py
+++ b/lmcache/v1/config.py
@@ -26,6 +26,11 @@ from lmcache.v1.config_base import (
     create_config_class,
     load_config_with_overrides,
 )
+from lmcache.v1.wings_ext.config import (
+    get_wings_extra_config,
+    get_wings_feature_config,
+    is_wings_feature_enabled,
+)
 
 logger = init_logger(__name__)
 
@@ -597,6 +602,18 @@ def _get_extra_config_value(self, key, default_value=None):
         return default_value
 
 
+def _get_wings_config(self):
+    return get_wings_extra_config(self)
+
+
+def _get_wings_feature_config(self, feature_name):
+    return get_wings_feature_config(self, feature_name)
+
+
+def _is_wings_feature_enabled(self, feature_name, default=False):
+    return is_wings_feature_enabled(self, feature_name, default)
+
+
 def _get_lmcache_worker_ids(self, use_mla, world_size):
     if not self.lmcache_worker_ids:
         # if mla is not enabled, return all worker ids, which means start
@@ -741,6 +758,9 @@ LMCacheEngineConfig = create_config_class(
     namespace_extras={
         "validate": _validate_config,
         "log_config": _log_config,
+        "get_wings_config": _get_wings_config,
+        "get_wings_feature_config": _get_wings_feature_config,
+        "is_wings_feature_enabled": _is_wings_feature_enabled,
         "get_extra_config_value": _get_extra_config_value,
         "get_lmcache_worker_ids": _get_lmcache_worker_ids,
         "get_lookup_server_worker_ids": _get_lookup_server_worker_ids,
